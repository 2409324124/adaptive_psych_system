services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - ipip_sessions:/app/data/sessions

volumes:
  ipip_sessions:
